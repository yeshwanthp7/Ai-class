# Security Standards

- API keys must reside in environment variables.
- Firestore operations protected by server-side security rules.
- Validate input lengths and characters on search bars and forms.
