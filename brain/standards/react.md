# React Standards

- Clean hooks usage (useCallback for callbacks, useMemo for expensive mappings).
- Component modularity (reusable UI blocks under src/components).
- Zero DOM manipulation outside react refs.
