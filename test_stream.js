const https=require('https');require('dotenv').config();const postData=JSON.stringify({model:'nvidia/llama-3.1-nemotron-70b-instruct',messages:[{role:'user',content:'Count to 3.'}],max_tokens:50,stream:true});const req=https.request({hostname:'integrate.api.nvidia.com',port:443,path:'/v1/chat/completions',method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer ' + process.env.NVIDIA_API_KEY,'Content-Length':Buffer.byteLength(postData)}},res=>{console.log('Status: '+res.statusCode);res.on('data',d=>process.stdout.write(d))});req.on('error',console.error);req.write(postData);req.end();
